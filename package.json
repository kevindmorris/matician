{
  "name": "matician",
  "version": "1.1.2",
  "description": "A modern JavaScript utility library for arithmetic, mathematics, probability, statistics, and more.",
  "homepage": "https://kevindmorris.github.io/matician/",
  "bugs": {
    "url": "https://github.com/kevindmorris/matician/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kevindmorris/matician.git"
  },
  "license": "MIT",
  "author": {
    "name": "Kevin Morris",
    "url": "https://github.com/kevindmorris"
  },
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "format": "prettier . --write",
    "test": "jest",
    "docs": "typedoc --out docs src",
    "predeploy": "npm run docs",
    "deploy": "gh-pages -d docs",
    "version:patch": "npm version patch",
    "version:minor": "npm version minor",
    "version:major": "npm version major",
    "publish:patch": "npm run build && npm run version:patch && npm run deploy && git push --follow-tags && npm publish",
    "publish:minor": "npm run build && npm run version:minor && npm run deploy && git push --follow-tags && npm publish",
    "publish:major": "npm run build && npm run version:major && npm run deploy && git push --follow-tags && npm publish"
  },
  "devDependencies": {
    "@jest/globals": "^30.0.3",
    "@types/jest": "^30.0.0",
    "gh-pages": "^6.3.0",
    "jest": "^30.0.3",
    "prettier": "^3.2.5",
    "ts-jest": "^29.4.0",
    "tsup": "^8.0.2",
    "typedoc": "^0.28.5",
    "typescript": "^5.8.3"
  }
}
