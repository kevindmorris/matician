/**
 * Compute the product of two numbers.
 * @param {number} multiplier A number by which a number is multiplied.
 * @param {number} multiplicand A number which is multiplied by another.
 * @returns {number} Returns the product. 
 */
export default function multiply(multiplier: number, multiplicand: number): number {
  return multiplier * multiplicand;
}
