/**
 * Compute the absolute value of a number.
 *
 * @since 0.5.0
 * @param {number} x The number.
 * @returns {number} Returns the absolute value of of `x`.
 *
 * @example
 * abs(-2)
 * // => 2
 */
export default function abs(x: number): number {
  return Math.abs(x);
}
