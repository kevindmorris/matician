import standardDeviation from "./standardDeviation";

describe("standardDeviation", () => {
  it("handles basic cases", () => {
    // Add your own values as needed
    expect(() => standardDeviation([])).toThrow();
  });
});
