/**
 * Volume of a cube.
 *
 * @since 0.7.0
 * @param {number} a The length of a size.
 * @returns {number} Returns the volume.
 *
 * @example
 * volume.cube(2)
 * // => 8
 */
export default function cube(a: number): number {
  return a * a * a;
}
