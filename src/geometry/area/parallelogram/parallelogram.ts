/**
 * Area of a parallelogram.
 *
 * @since 0.7.0
 * @param {number} b
 * @param {number} h
 * @returns {number} Returns the area.
 *
 * @example
 * area.parallelogram(3, 2)
 * // => 6
 */
export default function parallelogram(b: number, h: number): number {
  return b * h;
}
