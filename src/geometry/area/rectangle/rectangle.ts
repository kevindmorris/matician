/**
 * Area of a rectangle.
 *
 * @since 0.7.0
 * @param {number} a
 * @param {number} b
 * @returns {number} Returns the area.
 *
 * @example
 * area.rectangle(2, 3)
 * // => 6
 */
export default function rectangle(a: number, b: number): number {
  return a * b;
}
