/**
 * Area of a square.
 *
 * @since 0.7.0
 * @param {number} a
 * @returns {number} Returns the area.
 *
 * @example
 * area.square(2)
 * // => 4
 */
export default function square(a: number): number {
  return a * a;
}
